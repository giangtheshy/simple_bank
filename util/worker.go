package util

func worker() {
	// ctx := context.Background()
	// for i := 0; i < 10; i++ {
	// 	time.Sleep(1 * time.Second)
	// 	arg.Username = fmt.Sprintf("%s%d", arg.Username, i)
	// 	arg.Email = fmt.Sprintf("%s%d", arg.Email, i)
	// 	user, err := db.StoreDB.CreateUser(ctx, arg)
	// 	if err != nil {
	// 		fmt.Println(err)
	// 	}
	// 	fmt.Println(user)
	// }
}